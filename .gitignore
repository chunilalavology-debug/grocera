# Dependencies
node_modules
node_modules/
*/node_modules/

# Local env (never commit secrets)
.env
.env.local
.env.*.local
backend/.env
frontend/.env.production

# Firebase / GCP keys (never commit)
backend/utils/serviceAccount.json
**/serviceAccount.json

# Local SQLite (runtime / dev)
zippyyy-ships/**/server/data/*.sqlite
zippyyy-ships/**/server/data/*.sqlite-shm
zippyyy-ships/**/server/data/*.sqlite-wal
backend/zippyyy-ships-server/data/*.sqlite
backend/zippyyy-ships-server/data/*.sqlite-shm
backend/zippyyy-ships-server/data/*.sqlite-wal

# CRA production build — Vercel runs `npm run build`; do not commit output
frontend/build/
dist/
.cache/
coverage/
*.log
.DS_Store
Thumbs.db

# Vercel CLI local link metadata
.vercel
backend/.vercel
frontend/.vercel

