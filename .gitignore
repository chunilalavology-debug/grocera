# Dependencies
node_modules
node_modules/
*/node_modules/

# Local env (never commit secrets)
.env
vps.env
.env.local
.env.*.local
backend/.env
frontend/.env.production
frontend/.env.development.local
frontend/.env.production.local
frontend/zippyyy-ships-ui/.env
frontend/zippyyy-ships-ui/.env.local
frontend/zippyyy-ships-ui/.env.production
frontend/zippyyy-ships-ui/.env.production.local

# Firebase / GCP keys (never commit)
backend/utils/serviceAccount.json
**/serviceAccount.json

# Local SQLite (runtime / dev) — ships API lives in backend/zippyyy-ships-server
backend/zippyyy-ships-server/data/*.sqlite
backend/zippyyy-ships-server/data/*.sqlite-shm
backend/zippyyy-ships-server/data/*.sqlite-wal

# CRA production build — Vercel runs `npm run build`; do not commit output
frontend/build/
dist/
.cache/
coverage/
*.log

# Smoke / audit output (generated locally)
backend/script/reports/

.DS_Store
Thumbs.db

# Vercel CLI local link metadata
.vercel
backend/.vercel
frontend/.vercel

