{
  "name": "grocera-monorepo",
  "private": true,
  "description": "Run backend + frontend together for local development",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend -c blue,magenta \"npm start --prefix backend\" \"wait-on -t 120000 tcp:127.0.0.1:5000 && npm start --prefix frontend\"",
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wait-on": "^8.0.1"
  }
}
